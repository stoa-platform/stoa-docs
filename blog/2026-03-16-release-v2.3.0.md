---
slug: release-v2.3.0
title: "v2.3.0: Pingora Engine, Chat Settings, Zero-Config Bootstrap"
authors: [stoa-team]
tags: [release, announcement, feature, ai, architecture]
description: "Pingora connection engine, per-tenant chat settings with source tracking, zero-config Helm bootstrap, real-data API seeding, and OAuth hairpin NAT fix."
keywords:
  - STOA release
  - STOA v2.3.0
  - Pingora gateway
  - chat settings API gateway
  - Helm bootstrap job
  - MCP gateway release
  - AI gateway release notes
  - open source API gateway 2026
---

# STOA v2.3.0

Pingora's battle-tested connection engine meets AI-native API management — plus zero-config bootstrap, per-tenant chat controls, and real-data demo APIs.

<!-- truncate -->

## Highlights

### Pingora Connection Engine (ADR-058)

STOA Gateway now embeds [Pingora](https://github.com/cloudflare/pingora), Cloudflare's proxy framework handling 1T+ requests/day. Rather than a full migration (55+ pts), we chose the **embedded connector** approach (13 pts): Pingora's shared connection pool lives inside the existing axum binary behind a `pingora` feature flag.

- **Shared connection pool** — designed for 1T+ req/day, replaces per-client reqwest pools at scale
- **Zero architecture change** — same binary, same routing, same API surface
- **Feature-gated** — `FEATURES=kafka,pingora` in production builds (cmake required)
- **Future-proof** — `ProxyPhase` trait maps 1:1 to Pingora's `ProxyHttp` for a potential full migration at v2.0

> **Marketing claim**: "STOA Gateway — powered by Pingora's connection engine"

See [ADR-058](/docs/architecture/adr/adr-058-pingora-integration) for the full decision record.

### Per-Tenant Chat Settings & Source Tracking

Admins can now independently control the AI chat assistant in Console and Portal, with per-app usage tracking.

- **Per-tenant toggles** — enable/disable chat separately for Console and Portal
- **Daily token budget** — set spending limits per tenant to prevent runaway costs
- **Source tracking** — every chat message tagged with `X-Chat-Source` header (console or portal)
- **Usage breakdown** — `GET /v1/chat/usage?group_by=source` shows per-app token consumption
- **RBAC enforced** — only `cpi-admin` and `tenant-admin` can modify settings; `viewer` gets 403
- **25 E2E scenarios** — CRUD, RBAC, source header enforcement, cross-app integration

New endpoints:

| Method | Path | Description |
|--------|------|-------------|
| `GET` | `/v1/chat/settings` | Read tenant chat configuration |
| `PUT` | `/v1/chat/settings` | Update chat toggles and budget |

### Zero-Config Helm Bootstrap

First-time installations are now fully automated. A single `helm install` with bootstrap enabled provisions the entire platform:

- **Keycloak realm** — creates realm with registration enabled
- **4 RBAC roles** — `cpi-admin`, `tenant-admin`, `devops`, `viewer`
- **3 client scopes** — `stoa:read`, `stoa:write`, `stoa:admin`
- **4 OIDC clients** — Console, Portal, Gateway, API (all pre-configured)
- **Admin user** — created with `cpi-admin` role if `adminEmail` is set
- **Default tenant** — provisioned via Control Plane API

```bash
helm install stoa-platform ./charts/stoa-platform \
  --set stoa.bootstrap.enabled=true \
  --set stoa.domain=example.com \
  --set stoa.adminEmail=admin@example.com
```

The bootstrap job is **idempotent** — running it twice is safe (409 = already exists = OK).

### Shared Secrets Management

Internal service-to-service keys are now auto-generated and stable across upgrades:

| Key | Purpose |
|-----|---------|
| `gateway-consumer-key` | Gateway ↔ API authentication |
| `chat-internal-key` | Chat service internal calls |
| `jwt-signing-key` | JWT token signing |
| `anthropic-api-key` | Anthropic API (optional) |

Keys persist across `helm upgrade` via Helm `lookup` function. When Anthropic API key is not provided, chat gracefully disables via `CHAT_KILL_SWITCH`.

### Real-Data Demo APIs

The developer portal now ships with **5 real public APIs** pre-configured for immediate testing:

| API | Auth | Rate Limit | Use Case |
|-----|------|-----------|----------|
| Exchange Rate API | None | Unlimited | Foreign exchange rates (180+ currencies) |
| CoinGecko | None | 30 req/min | Cryptocurrency prices and market cap |
| OpenWeatherMap | API Key | 1,000/day | Real-time weather data |
| NewsAPI | API Key | 100/day | Top headlines from 80+ sources |
| Alpha Vantage | API Key | 5/min | Stock prices and technical indicators |

Plus an **echo backend** fallback for when external APIs are unreachable — demos never break.

---

## Gateway (Rust)

| Capability | Description |
|-----------|-------------|
| Pingora Engine | Embedded shared connection pool behind `pingora` feature flag |
| OAuth Hairpin Fix | Internal Keycloak URL for DCR/token proxy — fixes 502 on K8s clusters |
| Deployment Mode Tracing | `stoa.deployment_mode` span attribute for Tempo service graph |
| Chat Source Header | `X-Chat-Source` propagation and enforcement |

### OAuth Hairpin NAT Fix

OAuth proxy endpoints (token, DCR, PAR) were using the external Keycloak URL. On K8s clusters with hairpin NAT issues (e.g., OVH MKS), this caused 502 errors at the DCR registration step — breaking Claude.ai MCP connections.

**Fix**: New `Config::keycloak_backend_url()` prefers `STOA_KEYCLOAK_INTERNAL_URL` (K8s service DNS) for backend calls while keeping the external URL for browser-facing discovery fields.

## Control Plane API (Python/FastAPI)

| Capability | Description |
|-----------|-------------|
| Chat Settings | `GET/PUT /v1/chat/settings` — per-tenant toggles + budget |
| Source Tracking | `ChatSource` enum, `X-Chat-Source` header, `group_by=source` |
| API Seeding | Alembic migration 074 — 5 real APIs + echo fallback |
| Chat Budget | Per-tenant daily token limits with enforcement |

## Console & Portal (React/TypeScript)

- **Chat Settings page** — toggle chat per-app, set daily budget, save flow
- **Source header injection** — `useChatService` hook sends `X-Chat-Source` on all requests
- **RBAC gates** — viewer role blocked from settings modification

## Helm Chart

New values:

| Key | Default | Description |
|-----|---------|-------------|
| `stoa.domain` | — | Base domain for all services |
| `stoa.adminEmail` | — | Admin user email (bootstrap) |
| `stoa.anthropicApiKey` | — | Anthropic API key (optional, enables chat) |
| `stoa.bootstrap.enabled` | `false` | Enable first-time platform provisioning |

---

## Bug Fixes

- **Portal 502** — Corrected `API_BACKEND_URL` and added `DNS_RESOLVER` in deployment manifest (#1813)
- **OAuth DCR 502** — Gateway now uses internal Keycloak URL for backend proxy calls (#1814)
- **Tempo service graph** — Deployment mode as span attribute (not resource attribute) for correct extraction (#1811)

## Breaking Changes

None. All changes are backwards compatible.

## Upgrade Guide

```bash
# Standard Helm upgrade (no bootstrap)
helm upgrade stoa-platform ./charts/stoa-platform -n stoa-system

# First-time install with bootstrap
helm install stoa-platform ./charts/stoa-platform -n stoa-system \
  --set stoa.bootstrap.enabled=true \
  --set stoa.domain=yourdomain.com \
  --set stoa.adminEmail=admin@yourdomain.com
```

Gateway Docker images now require `cmake` at build time (Pingora dependency). Pre-built images from GHCR include this — no action needed for standard deployments.

## What's Next

- **eBPF kernel-level HTTP parsing** — zero-copy request inspection (CAB-1841/1843)
- **UAC policy enforcement at kernel level** — eBPF-based policy evaluation (CAB-1848)
- **WASM plugin runtime** — user-defined plugins in WebAssembly (CAB-1644)
- **MCP Federation** — cross-gateway tool discovery (ADR-046)

See the full [roadmap](/docs/roadmap) for planned features and milestones.

## Resources

- [Documentation](https://docs.gostoa.dev)
- [GitHub](https://github.com/stoa-platform/stoa)
- [ADR-058: Pingora Integration](/docs/architecture/adr/adr-058-pingora-integration)
- [Discord](https://discord.gostoa.dev)
